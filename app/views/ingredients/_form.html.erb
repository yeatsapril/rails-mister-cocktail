<div class="container p-4"><%= simple_form_for(ingredient) do |form| %>
  <%= form.input :name, placeholder: 'Type the name..' %>
  <%= form.submit action_name == 'new' ? 'save ingredient' : 'edit ingredient', class: "btn btn-success" %>
  <% end %>
</div>
